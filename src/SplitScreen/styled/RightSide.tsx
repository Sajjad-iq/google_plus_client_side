import styled from "styled-components";

export const RightSide = styled.section`
 width: 100%;
 overflow: scroll;
 position: relative;
  background:white;

   &::-webkit-scrollbar {
  display: none;
};
`